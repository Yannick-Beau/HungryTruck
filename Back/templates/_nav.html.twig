<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('back_office_home') }}">HungryTruck</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('back_office_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('back_office_user_index') }}">Utilisateur</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('back_office_foodtruck_index') }}">FoodTruck</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('back_office_category_food_index')}}">Catégories de nourritures</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('back_office_event_foodtruck_index')}}">Evenements FoodTruck</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('back_office_faq_index')}}">FAQ</a>
				</li>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<li class="nav-item">
						<a class="btn btn-danger" href="{{ path('app_logout')}}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="btn btn-success" href="{{ path('app_login') }}">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>

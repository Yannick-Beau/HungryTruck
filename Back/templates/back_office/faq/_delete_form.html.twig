<form method="post" action="{{ path('back_office_faq_delete', {'id': faq.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faq.id) }}">
    <button class="btn">Delete</button>
</form>

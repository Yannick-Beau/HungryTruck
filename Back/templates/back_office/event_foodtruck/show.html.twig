{% extends 'base.html.twig' %}

{% block title %}BackOffice - Liste EventFoodtruck{% endblock %}

{% block body %}
    <h1>Liste EventFoodtruck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_foodtruck.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ event_foodtruck.day }}</td>
            </tr>
            <tr>
                <th>Hours</th>
                <td>{{ event_foodtruck.hours }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ event_foodtruck.place }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ event_foodtruck.createdAt ? event_foodtruck.createdAt|date('Y-m-d H:i') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ event_foodtruck.updatedAt ? event_foodtruck.updatedAt|date('Y-m-d H:i') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_office_event_foodtruck_index') }}" class="btn btn-primary"><i class="fas fa-undo-alt"></i></a>

    <a href="{{ path('back_office_event_foodtruck_edit', {'id': event_foodtruck.id}) }}" class="btn btn-warning mx-2"><i class="far fa-edit"></i></a>

    {{ include('back_office/event_foodtruck/_delete_form.html.twig') }}
{% endblock %}
